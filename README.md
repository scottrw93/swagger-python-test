A python client uploading files to a spring-boot server

To run the server:

    mvn clean package
    cd target
    java -jar cdmfm-rest-api.jar

Then there is a test script in the python client directories uploading files
